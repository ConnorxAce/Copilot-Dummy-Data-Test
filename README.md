# Firewall Telemetry Dataset

This repository contains a simulated firewall monitoring dataset.

The environment contains 30 firewall devices named firewall1 through firewall30.

Each firewall record includes:
- CPU utilization
- Memory utilization
- Availability percentage
- Uptime hours

The telemetry dataset is stored in the file:

data/firewalls.json
